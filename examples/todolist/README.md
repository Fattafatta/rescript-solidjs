# rescript-solidjs-example

Example implementation of a todo list using `ReScript`, `SolidJS` and `vite`.

## Usage

Install packages:

```shell
npm install

# or

yarn
```

Start `vite` server:

```shell
npm run start
```
